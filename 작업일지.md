# 작업일지

새 날짜를 **맨 위**에 추가한다. 형식: 한 일 / 알게 된 것 / 다음.
QR게임(`nori/`)의 경과는 [nori/README.md](nori/README.md)에 적는다. 여기엔 날짜와 한 줄만.

---

## 2026-09-17 — 문서 정리, 커밋 기록 다시 쓰기

- **문서를 나눔.** README(492줄)를 현황 문서로 줄이고, 수집 규칙·화면 절은 내용 그대로 `docs/수집규칙.md`·`docs/화면.md` 로 옮김(빠진 줄 0 확인). 옛 기준이 된 곳(휴양림 96곳·TourAPI 여덟 지역 등)엔 지금 기준 안내를 덧붙임.
- Claude 메모리에만 있던 판단 기준·디자인 규칙·"다시 찾지 말 것"을 `CLAUDE.md` 로 옮김. 이 작업일지를 새로 만듦(8/13~).
- README 현황을 data.json(2026-09-15 수집) 실제 수치로 고침: 행사 710 · 예약 543 · 휴양림 186 · 난지캠핑장 25 · 키즈카페 133.
- 부르는 이름을 정함: "위캔고"(주말 앱), "QR게임"(nori).
- **커밋 기록 전체를 다시 써서 강제로 올림**(사용자 요청, 옛 기록 정리). 83개 커밋 번호가 전부 바뀜, 최신 파일은 그대로. 다시 배포 성공, 두 주소 모두 200 확인. 다른 PC 사본은 새로 받아야 함.
- QR게임: 사지선다 네 놀이 10문제씩·무작위 3문제, 제목 "즐거운 QR게임" 등 4건.

## 2026-09-15 — 휴양림 탭에 달력과 난지캠핑장

- 휴양림 탭에 달력과 난지캠핑장 칸을 넣음(`data.json` 의 `camping`). 고른 날짜에 이용 못 하는 구역은 숨김. 캠핑장만 넣기로 사용자가 정함.
- 서울 공공서비스예약 남은 자리는 차단 장치에 막혀 못 가져옴(확인 완료).

## 2026-09-10 ~ 09-14 — QR게임(nori) 만들기

- 어르신 퀴즈 앱으로 시작 → 부스 도장 모으기로 확장(`nori/`). 자세한 경과는 nori/README.md.

## 2026-08-24 — 사람이 고른 장소 자리 논의

- 사용자가 "공공데이터보다 인스타가 낫다"고 함. 인스타 자동 수집은 불가 → `collect.js` 의 `MANUAL` 에 손으로 넣기로 방향만 잡음(아직 비어 있음).

## 2026-08-22 — 수집은 전국, 자르는 건 화면

- 100km 상자 → 300km 반경 → 수집 단계 자르기를 아예 없앰. 화면이 출발지에서 300km(`MAX_KM`)로 자름.
- 출발지를 서울 25개 자치구에서 **17개 시·도**로 바꿈(사용자: "너무 자세하게 하지 말고 도별로").
- 상시 시설을 내보내지 않기로(`EMIT_PLACES = false`, 사용자: "다시 생각해보겠다").
- TourAPI가 분류 체계를 갈아타 올해 등록 관광지가 빠지는 걸 발견(아직 안 고침).

## 2026-08-21 — 휴양림 편의시설·사진, 탭 전체에 지역 체크

- 숲나들e 예약검색을 긁어 휴양림에 편의시설 배지와 사진을 붙임.
- 휴양림·키즈카페 탭도 찾기 탭의 지역 체크를 따르게 함.

## 2026-08-20 — 키즈카페 함수 배포

- 남은 자리를 세는 함수 `slots` 배포. 서버가 UTC라 토요일이 금요일로 밀리던 것을 `getUTCDay()` 로 고침.
- 키즈카페를 회차별로 보여주고 합계 자리수는 뺌. 지점 칸을 이름·회차·예약 버튼으로 나눔.
- 공유누리 API 호출법을 뚫음(POST+JSON). 앱에 넣을지는 미결.

## 2026-08-15 — 키즈카페 탭, 버튼 디자인, 권역 3단

- 서울형 키즈카페 133곳 전용 탭과 중계 함수(배포 전)를 만듦.
- 분류 배지를 빼고 자세히·지도를 버튼으로. 버튼을 posthog.com 실물처럼 채움·턱·아웃라인 세 색으로 맞춤.
- 권역이 클 때 시군구도 접는 3단 구조, 자동 펼침 없앰, 찾기 탭 소제목을 묻는 말로.
- 상업 화랑·작가 작업실을 이름으로 제외.

## 2026-08-14 — 경기 시군 예약, 상시 시설 걷어내기

- 경기도 시군 예약을 붙임(남양주·김포·수원·안산 등, 시군 하나에 파일 하나, `sigun/지침.md`). 김포 링크에 `rep=1`.
- 상시 시설이 1,716곳까지 불어 예약 칸을 덮어 694곳으로 걷어냄(사립·대학 박물관, 자연물 10종, 전국관광지정보 통째 등 — 사용자와 하나씩 정함).
- 지역 체크와 목록을 권역 단위로 묶음. 자료가 박힌 옛 미리보기 사본(`preview.html`) 삭제.

## 2026-08-13 — 첫 커밋

- 서울 공공서비스예약·표준데이터·TourAPI를 모으는 수집기와 화면으로 시작. GitHub Pages로 배포.
- PostHog 계열 크림+주황 화면, 휴대폰 화면 맞춤, 지역·요금 필터, 상시 시설 개수 상한 없앰.
- 링크 없는 카드를 구석구석 링크로 채웠다가 1,029건이 전부 죽은 링크라 걷어내고 지도 링크로. 휴양림 카드에 숲나들e 링크.
