# CLAUDE.md — 주말 어디 가지 ("위캔고")

사용자는 비개발자 관리자 — 한국어로, 기술 용어는 풀어서. **개인 앱**이다(다모인 업무 앱과 섞지 않는다).

@README.md

> 위 임포트로 README(현황 원본)가 자동으로 들어온다. 현황은 README에만 쓴다.
> **QR게임(`nori/`) 작업이면 `nori/README.md` 부터 읽는다.** 이 파일의 규칙은 주말 앱 것이다.

## 공개 저장소다

- 누구나 읽는다. **`keys.json` 은 절대 커밋하지 않는다**(.gitignore 에 있다). 문서·커밋 메시지에 키 값을 적지 않는다.
- **제3자 계정 이름을 문서·커밋 메시지에 적지 않는다.** 사용자의 GitHub 계정은 nswf301 하나다.
- 개인 구글 메일(Firebase 배포 계정)은 커밋 작성자로 이미 공개돼 있어 문서에 둬도 된다고 사용자가 정했다(2026-09-17).
- 커밋 전에 `git status` 로 임시 파일·미리보기 사본이 쓸려 들어가지 않는지 본다.

## 판단 기준 — 사용자가 정한 것 (어기면 "산으로 간다")

1. **목적은 예약을 편하게다.** 제안·작업은 "예약 칸을 채우는가"로 판단한다. 장소 데이터를 늘리는 쪽으로 끌고 가지 않는다. — 2026-08-13 작업이 목록 늘리기로 흘러 예약 칸은 그대로인데 상시 시설만 614 → 1,719건이 됐다.
2. **자료를 고르게 할 땐 API가 주는 전체를 먼저 보여주고 사용자가 고르게 한다.** 내가 미리 3등분해서 애매한 것만 묻지 않는다. — 그렇게 했다가 사용자가 고르지 않은 601건이 임의로 들어갔다.
3. **수집 단계에서 거리로 자르자고 제안하지 않는다.** 수집은 전국, 자르는 건 화면이다(2026-08-22). 수집 때 서울 기준으로 자르면 출발지를 바꿔도 소용이 없다.
4. [docs/수집규칙.md](docs/수집규칙.md)의 거르는 규칙은 **하나씩 합의한 것**이다. 임의로 되돌리지 않는다.
5. 카드의 대상 문구는 **원문 그대로** 보여준다. 잘라 쓰지 않는다.

## 디자인

- 본보기는 **PostHog** — 크림 바탕 `#EEEFE9` + 주황 `#F54E00`, 그림자 대신 1px 테두리.
- **버튼은 색이 셋**이다 — 채움 `--btn-face` / 턱 `--btn-lip` / 아웃라인 `--btn-line`. 사용자가 posthog.com을 확대해가며 세 번 고쳐 맞췄다. **단색으로 되돌리지 않는다.** 흰 버튼(`.go.map`)의 턱만 `--btn-lip-2`.
- 이모지 대신 **파일 안에 그린 SVG 선 아이콘**(기기마다 그림이 달라지지 않게). 찾기 탭 소제목 넷은 묻는 말이다.
- 카드 안 글자는 16px. **자동 펼침 없음** — 처음엔 섹션 제목만 보인다.
- 키즈카페 지점 이름을 링크나 버튼으로 만들지 않는다(2026-08-20에 한 번 그랬다가 되돌림).
- 자세한 화면 규칙은 [docs/화면.md](docs/화면.md).

## 검증 요령

- **외부 링크는 상태 코드만 보지 말고 내용까지 연다**(제목·본문 길이). 오류 화면도 200으로 오는 사이트가 있다. — 구석구석 1,029건을 죽은 링크로 만들었다가 걷어냈고, 김포는 `rep=1` 이 빠져 상세가 안 열렸다.
- 배포 확인은 `gh run list -R nswf301/weekend-go`. PowerShell에서 `gh ... --jq '...'` 는 따옴표가 깨지니 Bash로.
- 함수에서 날짜·요일은 UTC 기준(`getUTCDay()`). 사용자 PC(한국 시간)에서 통과해도 서버에서 하루 밀린다.

## 다시 찾지 말 것 (확인 완료)

- **모집정원·잔여석** — 어느 API에도 없다. 서울은 상세설명 글 속에 9%만 있고 그나마 "회차당 1기관" 같은 형태다. 공연행사 `seatNumber` 는 공연장 좌석수지 남은 자리가 아니다.
- **휴양림 실시간 빈자리** — 공개 API로는 못 얻는다(2026-08-16 공공데이터포털 "휴양림" API 9건 전수 확인: 산림청 예약정보 API는 과거 기록, "실시간"은 방치된 한 곳). API 쪽은 다시 파지 않는다. 숲나들e 홈페이지 예약검색을 긁어 편의시설·사진은 붙였지만 빈자리는 로그인이 필요해 못 붙인다.
- **숲나들e hmpgId를 손으로 긁지 않는다** — 산림청이 "홈페이지구성ID정보"로 공개하고 있다.
- **대한민국구석구석 소개 페이지를 contentid로 만들기** — 안 된다. 구석구석의 `cotid` 는 TourAPI `contentid` 와 **다른 번호 체계**이고, 오류 화면도 **HTTP 200**으로 온다. 진짜 홈페이지가 필요하면 `detailCommon2` 를 항목당 한 번씩 불러야 하는데 하루 한도(1,000회)를 넘는다. 사용자 결정으로 **지도 링크만 쓴다.**
- **경기공유서비스(share.gg.go.kr)** — 31개 시군 통합 예약이지만 목록이 자바스크립트라 직접은 못 긁는다. 공유누리 API가 그 상세로 바로 가는 링크를 준다(README 다음 할 일 3).
- **경기도 통합 예약 API는 없다.** 시군이 각자 굴린다.
- **평생학습강좌 표준데이터는 넣지 않는다** — 성인 강좌뿐이다.
- **농어촌체험휴양마을은 넣지 않는다** — 한 번 넣었다 뺐다. 제대로 운영되는 곳이 드물다는 사용자 판단.
- **TourAPI 축제(`searchFestival2`) 0건은 코드 문제가 아니다** — 관광공사 축제 자료가 2026년 3월쯤에서 끊긴다.
- **인스타그램 자동 수집은 안 된다** — 로그인 벽·요청 제한·약관 금지, Graph API는 본인 계정 게시물만 준다. **우회로를 찾자고 제안하지 않는다.** 대신 `MANUAL` 에 손으로 넣는다.
- **서울 공공서비스예약의 남은 자리(난지캠핑장 등)** — 차단 장치에 막혀 못 가져온다(2026-09-15 확인). 사용자는 "어쩔 수 없다"고 했다.
- **키즈카페 예약 달력을 하루까지 찍는 주소** — `q_resveDe` 를 붙여도 서버가 무시한다(2026-08-20 확인). 연·월까지만 된다.
- **로그인 벽이 있는 지자체 예약처** — 자동 수집 불가. 남양주는 로그인이 필요 없다는 걸 확인해 자동 수집으로 옮겼다([docs/수집규칙.md](docs/수집규칙.md) 「경기도 시군 예약」).

## 기록 규칙

- **현황은 README**, 규칙·다시 찾지 말 것은 **이 파일**, 코드가 어떻게 도는지는 **docs/**, 날짜별 경과는 **작업일지.md**, QR게임은 **nori/README.md**.
- 작업이 끝나면: `작업일지.md` 맨 위에 날짜 항목 → README 현황·다음 할 일 갱신 → 커밋+푸시.
- 문서를 옮기거나 나눌 땐 "위/아래 ○○ 참고" 같은 가리킴이 끊기지 않았는지 찾아 고친다.
